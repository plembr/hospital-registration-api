services:
  registration-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DATABASE_PATH: /app/data/hospital_mock.db
    volumes:
      - ./data:/app/data
